{
  "name": "reselect-selectors",
  "version": "2.2.1",
  "description": "Collection of selectors which are needed in each repository which uses reselect",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest ./src",
    "build": "rm -rf ./lib && ./node_modules/.bin/babel -e 0 ./src --out-dir ./lib",
    "prepare": "npm test && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/jsifalda/reselect-selectors.git"
  },
  "keywords": [
    "reselect",
    "selectors"
  ],
  "author": "sifalda.jiri@gmail.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jsifalda/reselect-selectors/issues"
  },
  "homepage": "https://github.com/jsifalda/reselect-selectors#readme",
  "dependencies": {
    "lodash.get": "^4.4.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "jest": "^23.6.0"
  }
}
